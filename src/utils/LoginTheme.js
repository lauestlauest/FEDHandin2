import { createTheme } from "@mui/material/styles";

const loginTheme = createTheme({
  palette: {
    primary: {
      main: "#3f51b5",
    },
    secondary: {
      main: "#f50057",
    },
  },
});

export default loginTheme;
