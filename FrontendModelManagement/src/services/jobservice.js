import React from "react";

// Managers can:
// Create a new job
// Add model(s) to a job - more than one model can be added to a job
// Delete model(s) from a job
// Get a list of ALL jobs

// Models can:
// Get a list of jobs they are assigned to
// Add expenses to a job

const JobService = () => {
  return <div>JobService</div>;
};

export default JobService;
